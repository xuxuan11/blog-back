spring:
  datasource:
    driver‐class‐name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    com.xxclass.blog: debug
  file:
    name: blog-dev.log
    path: log/

